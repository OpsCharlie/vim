return {
    "j-hui/fidget.nvim",
    -- Extensible UI for Neovim notifications and LSP progress messages.
    lazy = true,
    event = { "BufReadPost", "BufNewFile" },
    opts = {
        -- options
    },
}
