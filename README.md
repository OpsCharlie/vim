# vim

The master branch is old and unmaintained.
The following branches are actively maintained:

- fern    : Minimal vim setup with fern file manager and vimscript config.
- all_lua : Full-featured lua config with nvim-tree file manager.
- snacks  : Snack replacement for several plugins (most recent).
